apiVersion: batch/v1
kind: Job
metadata:
  name: init-database
spec:
  ttlSecondsAfterFinished: 600  # Auto-cleanup after 10 minutes
  template:
    metadata:
      labels:
        app: init-db
    spec:
      restartPolicy: Never
      imagePullSecrets:
        - name: harbor-registry-secret
      initContainers:
        # Wait for PostgreSQL to be ready
        - name: wait-for-postgres
          image: harbor.corp.vmbeans.com/library/postgres:14-alpine
          command:
            - sh
            - -c
            - |
              echo "Waiting for PostgreSQL..."
              until pg_isready -h postgres-service -U bookstore_user; do
                echo "PostgreSQL is unavailable - sleeping"
                sleep 2
              done
              echo "PostgreSQL is up!"
          env:
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: DB_PASSWORD
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            runAsNonRoot: true
            runAsUser: 1000
            seccompProfile:
              type: RuntimeDefault
        
        # Run migrations using psql from postgres image (has psql built-in)
        - name: run-migrations
          image: harbor.corp.vmbeans.com/library/postgres:14-alpine
          command:
            - sh
            - -c
            - |
              echo "Running migrations..."
              
              # Run schema migration
              echo "Applying 001_schema.sql..."
              PGPASSWORD=$DB_PASSWORD psql -h postgres-service -U $DB_USER -d $DB_NAME -f /migrations/001_schema.sql || echo "Schema may already exist"
              
              # Run seed data migration
              echo "Applying 002_seed_books.sql..."
              PGPASSWORD=$DB_PASSWORD psql -h postgres-service -U $DB_USER -d $DB_NAME -f /migrations/002_seed_books.sql
              
              echo "Migrations complete!"
          env:
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: DB_PASSWORD
            - name: DB_NAME
              value: "bookstore"
          volumeMounts:
            - name: migrations
              mountPath: /migrations
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            runAsNonRoot: true
            runAsUser: 1000
            seccompProfile:
              type: RuntimeDefault
        
        # Wait for MinIO to be ready (using postgres image which is already in Harbor)
        - name: wait-for-minio
          image: harbor.corp.vmbeans.com/library/postgres:14-alpine
          command:
            - sh
            - -c
            - |
              echo "Waiting for MinIO..."
              # wget is available in alpine base image
              until wget -q --spider http://minio-service:9000/minio/health/live 2>/dev/null; do
                echo "MinIO is unavailable - sleeping"
                sleep 2
              done
              echo "MinIO is up!"
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            runAsNonRoot: true
            runAsUser: 1000
            seccompProfile:
              type: RuntimeDefault
      
      containers:
        # Seed images from Gutenberg
        - name: seed-images
          image: harbor.corp.vmbeans.com/bookstore/app:latest
          command: ["/app/scripts/bin/seed-images"]
          env:
            - name: DB_HOST
              value: "postgres-service:5432"
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: DB_PASSWORD
            - name: DB_NAME
              value: "bookstore"
            - name: MINIO_ENDPOINT
              value: "minio-service:9000"
            - name: MINIO_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: MINIO_ACCESS_KEY
            - name: MINIO_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: MINIO_SECRET_KEY
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
            runAsNonRoot: true
            runAsUser: 1000
            seccompProfile:
              type: RuntimeDefault
      volumes:
        - name: migrations
          configMap:
            name: migrations-configmap
