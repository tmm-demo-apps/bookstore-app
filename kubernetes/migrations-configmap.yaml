apiVersion: v1
kind: ConfigMap
metadata:
  name: migrations-configmap
  namespace: bookstore
data:
  # This ConfigMap is auto-generated. Do not edit manually.
  # To regenerate, run from the project root:
  # kubectl create configmap migrations-configmap --from-file=migrations/001_schema.sql --from-file=migrations/002_seed_books.sql -n bookstore --dry-run=client -o yaml > kubernetes/migrations-configmap.yaml
  #
  # Note: The migrations are also included in the Docker image at /app/migrations/
  # This ConfigMap provides an alternative way to mount migrations for init jobs.
