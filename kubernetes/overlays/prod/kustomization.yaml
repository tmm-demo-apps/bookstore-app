apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: bookstore

resources:
  - ../../base

patches:
  - path: configmap-patch.yaml
  - path: httproute-patch.yaml
  - path: tls-patch.yaml
