# Reader Application - EPUB library reader
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: reader
  namespace: infra-fbhdn
  labels:
    app.kubernetes.io/part-of: demo-suite
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    notifications.argoproj.io/subscribe.on-sync-succeeded.slack: demo-alerts
    notifications.argoproj.io/subscribe.on-sync-failed.slack: demo-alerts
spec:
  project: default
  
  source:
    repoURL: https://github.com/tmm-demo-apps/reader-app
    targetRevision: main
    path: kubernetes/overlays/prod
  
  destination:
    # vks-argo workload cluster
    name: vks-argo
    namespace: reader
  
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  
  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas
