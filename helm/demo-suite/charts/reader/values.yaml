enabled: true

image:
  repository: ghcr.io/tmm-demo-apps/reader-app
  tag: latest
  pullPolicy: IfNotPresent

replicas: 2

resources:
  requests:
    memory: "128Mi"
    cpu: "100m"
  limits:
    memory: "512Mi"
    cpu: "500m"

ingress:
  host: ""

postgres:
  image: ghcr.io/tmm-demo-apps/postgres:14-alpine
  storage: 5Gi
  resources:
    requests:
      memory: "256Mi"
      cpu: "100m"
    limits:
      memory: "512Mi"
      cpu: "500m"

# Pre-seed EPUBs into MinIO via init container.
# Enable for restricted networks where the reader can't download from mirrors at runtime.
# When disabled, the reader downloads EPUBs on-demand from the Gutenberg mirror.
epubSeed:
  enabled: false
  image: ghcr.io/tmm-demo-apps/reader-epubs:v1

secrets:
  create: true
  postgresPassword: reader-secret-pw
  databaseUrl: "postgres://reader:reader-secret-pw@reader-postgres:5432/reader?sslmode=disable"
  minioAccessKey: minioadmin
  minioSecretKey: minioadmin-secret
  sessionSecret: reader-session-secret-change-me
