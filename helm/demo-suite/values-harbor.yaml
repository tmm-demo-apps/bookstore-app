# Harbor environment override values
# Usage: helm install demo ./helm/demo-suite -f ./helm/demo-suite/values-harbor.yaml
#
# Prerequisites:
#   kubectl create secret docker-registry harbor-registry-secret \
#     --docker-server=harbor-01a.vcf.lab \
#     --docker-username=YOUR_USER \
#     --docker-password=YOUR_PASSWORD \
#     -n bookstore
#   (repeat for reader and chatbot namespaces)

global:
  storageClassName: "vsan-default-storage-policy"
  domain: "apps.lab"
  imagePullSecrets:
    - name: harbor-registry-secret

bookstore:
  image:
    repository: harbor-01a.vcf.lab/bookstore/app
    tag: latest
  postgres:
    image: harbor-01a.vcf.lab/library/postgres:14-alpine
  redis:
    image: harbor-01a.vcf.lab/library/redis:7-alpine
  elasticsearch:
    image: harbor-01a.vcf.lab/library/elasticsearch:8.11.0
  minio:
    image: harbor-01a.vcf.lab/library/minio/minio:latest

reader:
  image:
    repository: harbor-01a.vcf.lab/bookstore/reader
    tag: latest
  postgres:
    image: harbor-01a.vcf.lab/library/postgres:14-alpine

chatbot:
  image:
    repository: harbor-01a.vcf.lab/bookstore/chatbot
    tag: latest
  ollama:
    image: ollama/ollama:latest
